<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorAccent"
    tools:context=".base.SurveyActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/survey_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar" />

    <android.support.v4.view.ViewPager
        android:id="@+id/survey_view_pager"
        android:layout_width="wrap_content"
        android:layout_height="300dp"
        android:layout_gravity="center"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:paddingEnd="@dimen/card_padding_right_left"
        android:paddingLeft="@dimen/card_padding_right_left"
        android:paddingRight="@dimen/card_padding_right_left"
        android:paddingStart="@dimen/card_padding_right_left" />

</android.support.design.widget.CoordinatorLayout>